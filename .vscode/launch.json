{
    "configurations": [
        {
            "console": "externalTerminal", // This was working previously with integratedTerminal, but became unstable. Now an externalTerminal is launched, but I'm not yet sure if it is stable. Somehow not behaving with integratedTerminal as Git Bash after first launch...
            "justMyCode": true,
            "name": "Python: main.py",
            "program": "${workspaceFolder}/main.py",
            "request": "launch",
            "type": "debugpy",
        },
        {
            "console": "externalTerminal", // This was working previously with integratedTerminal, but became unstable. Now an externalTerminal is launched, but I'm not yet sure if it is stable. Somehow not behaving with integratedTerminal as Git Bash after first launch...
            "justMyCode": true,
            "name": "Pytest: Current File",
            "program": "${file}",
            "request": "test", // for pytest - ignore linting warning,
            "type": "debugpy",
        },
    ],
    "version": "0.2.0"
}
